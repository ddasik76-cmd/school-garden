Implement city/county/district search on the restored ASOS application. Keep a separate explicit save action and preserve the existing browser data stores. Use a local administrative-region lookup so school-network geocoding is not required. Hide station selection from the primary form, show the automatically selected observation source, and verify startup and persistence before release.